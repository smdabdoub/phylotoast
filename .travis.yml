language: python
python:
  - "2.7"
# command to run tests
script:
  - python -m unittest discover test_biom_calc
# command for email notifications, set to no notifications via email
notifications:
    email: false
